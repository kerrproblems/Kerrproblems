# Provisional entries

Problems with `maturity: provisional` or `verification_state: editorial_revision_needed` should be read as **works in progress**.

Current examples include:

- **K-503** — Carter-operator uniqueness split into explicit variants (a)(b)(c); needs a pinned formulation.
- **K-510** — Ernst-domain uniqueness; PDE domain not literature-locked in-repo (`theorem_status: needs_review`).

Update this list as entries graduate to `well_scoped` with primary references.
